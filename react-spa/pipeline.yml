trigger:
- master

pool:
 vmImage: ubuntu-latest

stages:
  - stage: 'StageA'
    jobs:
      - job: 'JobA'
        steps:
          - script: echo 'Ascript'
             
                     
            

